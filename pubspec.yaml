name: turner_afinador
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dependency_overrides:
  audiofft:
    git:
      url: https://github.com/HiFi-LoFi/AudioFFT

  dart_fft:
    git:
      url: https://github.com/author/dart_fft.git


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
